LOCAL CLASS

everything is static;
NOTE That D is lazy enough s.t. range.filter!(pred).empty will be efficient [as soon as filter evaluates to true the result will be false; pred called as fewer times as possible]

Fields:
	private Session[] userSessions; //slice of user sessions
	private Tuple!(Session, StopWatch)[] activeSessions; //list of all active sessions and their watches

Methods:
public:
	const(Session[]) getSessions() 
	//create a Session, a stopwatch and start the watch
	void startSession(const ulong projID, const string description = "", Category category = Category.NoneCategory) 
	//stop a Session and update its duration
	void stopSession(const ulong projID) 
	//manually create a new session
	void createSession (const ulong projID,const string date, const string description = "", Category category = Category.NoneCategory)